"""Pure-numpy camera and pitch geometry. No GPU, no weights, no I/O.

Copied from pitch3d (see the header on each module) and expected to diverge — the whole reason
camlab is a separate repo is that the camera contract has to change here.
"""
