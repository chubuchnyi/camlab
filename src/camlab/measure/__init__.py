"""Measuring a camera against the video — the only thing in camlab that is not a claim.

`paint` finds the painted lines in a frame. `residual` scores a camera's projected pitch against
them. Everything else in this repo is checked with these two.
"""
