"""camlab — a camera-calibration prototype for broadcast and handheld football clips.

One question: where was the camera, where was it pointed, and what was the focal, on each frame.
Everything else — poses, physics, rendering, the generative tail — lives in pitch3d.

Spec: ../docs/camlab-spec.md in the pitch3d repo.
"""

__version__ = "0.0.1"
