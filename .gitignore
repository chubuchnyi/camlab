__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
runs/
samples/

# Agent scratch, and git WORKTREES live here — .claude/worktrees/perf is a second full
# checkout of this repo, so committing it would nest the repository inside itself.
.claude/
